# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# mobile (Expo / React Native)
mobile/node_modules/
mobile/.expo/
mobile/dist/
mobile/ios/
mobile/android/

# supabase
supabase/.temp/
supabase/.branches/
.worktrees/
scripts/.seed-cosmos-checkpoint.json
scripts/.retailer-sitemap-cache.json
scripts/.retailer-image-candidates.csv
scripts/.retailer-exact-matches.csv
scripts/.scrape-*

# local AI-tool skill installs — .agents/skills holds the real fetched
# content, every other dot-dir here is a per-tool symlink mirror of it
# (see b714dd61, which excluded the same list from Vercel uploads as
# "local tooling state, not app files" — this extends that to git).
.agents
.claude/skills
/skills
.codebuddy
.commandcode
.continue
.crush
.factory
.goose
.junie
.kilocode
.kiro
.kode
.mcpjam
.mux
.neovate
.openhands
.pi
.pochi
.qoder
.qwen
.roo
.superpowers
.trae
.windsurf
.zencoder
